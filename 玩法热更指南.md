# 新增玩法 · 热更操作指南

> 结论先行：**新增玩法不用出新 APK。**
> 写一个 `js/game-xxx.js`，跑一次 `node tools/gen-pack.mjs`，推上 GitHub，用户下次打开 App 就看到新玩法了。

---

## 1. 热更覆盖矩阵

| 内容 | 能否热更 | 做法 |
|---|---|---|
| 课文 / 单词 / 题目数据（`js/data-*.js`） | ✅ | 改文件 → `gen-pack` |
| 改现有玩法逻辑（`js/games.js` 里的 `startXxx`） | ✅ | 改文件 → `gen-pack` |
| **新增玩法（新文件）** | ✅ | 新建 `js/game-*.js` → `registerGame` → `gen-pack` |
| 图片 / 音频 / 字体（`img/`、`audio/`、`font/`） | ✅ | 放文件 → `gen-pack`（走 `assets.zip`，**无 5MB 限制**） |
| 样式（`css/style.css`） | ✅ | 改文件 → `gen-pack` |
| 首页文案、单元结构、星星规则（`js/app.js`） | ✅ | 改文件 → `gen-pack` |
| TV 大屏布局判定（`js/tv.js`、`js/tv-tune.js`） | ✅ | 改文件 → `gen-pack`（**不碰冻结的 `boot.js`**，用可热更的 `tv.js` 反向补上 `body.tv` 类） |
| **`js/boot.js`（热更引导器本身）** | ❌ | 必须出新 APK |
| 原生能力（新增 JS 桥、权限、语音识别语种、SDK 版本） | ❌ | 必须出新 APK |
| 应用图标 / 包名 / versionCode / 启动页 | ❌ | 必须出新 APK |

> `boot.js` 里标了「冻结文件」。它是从内置 assets 里加载的，永远不会从热更包里替换 ——
> 这条设计保证了：热更的 js 再怎么坏，也毁不掉「回退到内置版本」这条逃生通道。

---

## 2. 新增玩法：三步走

### 第 1 步：新建 `js/game-xxx.js`

```js
/* 玩法：xxx */
(function () {
  function startXxx() {
    var u = curUnit();
    // …出题、渲染、判分，照抄 games.js 里任一玩法的写法即可…
    gameShell('<div class="big-emoji">🎯</div><div class="prompt">题干</div>');
  }

  /* 这一行是热更的关键：id 会被 gen-pack.mjs 扫进资源包清单 */
  registerGame({
    id: "xxx",            // 唯一 id，与首页入口一一对应
    name: "玩法名",        // 首页卡片标题
    icon: "🎯",            // 首页卡片图标（emoji 即可）
    desc: "一句话说明",     // 首页卡片副标题
    start: startXxx        // 点进去执行的函数
  });
})();
```

命名规则只有两条：
- 文件必须叫 `js/game-*.js`（生成器按这个前缀扫描）
- 文件里必须有 `registerGame({ id:"…" })`（生成器从这行读 id）

### 第 2 步：生成资源包

```bash
node tools/gen-pack.mjs
```

输出里会列出扫到的玩法：

```
✅ 资源包已生成 → hot/pack   (app=english min_apk=5)
   build     7360f15588786cd5
   代码文件  14 个  → code.zip   47.3KB
   玩法      xxx
```

### 第 3 步：提交推送

```bash
git add -A && git commit -m "新增玩法：xxx" && git push
```

CI 会自动把 `hot/pack/` 发布到 GitHub Pages。用户端：打开 App → 启动成功 3 秒后后台静默下载 → **下次打开生效**（首页多出一张卡片）。

### 第 4 步（如需三科都出现）：复制玩法文件到三科目录

`registerGame` 只认**当前学科目录**下的 `js/game-*.js`。某玩法只放在 `math/js/` 下，就只会出现在数学科首页；语文、英语不会出现。要让三科都上线，**同一 id 的文件各放一份**：

```bash
cp math/js/game-xxx.js cn/js/game-xxx.js
cp math/js/game-xxx.js en/js/game-xxx.js
# 然后重新 gen-pack，清单会列出 arena2, battle, arena2, battle, arena2, battle（数学/语文/英语 各一次）
node tools/gen-pack.mjs
```

> 三科共用同一 `id`（如 `arena2`）即可，内容互不影响；各自目录的 `img/`、`audio/` 也按学科前缀放（见 §6 坑 1）。

---

## 3. 它为什么能不用出 APK

```
APK 内置 assets                ← 第 1 层：永远可用，无网也能玩
  ↓
files/hot/（上次装的资源包）    ← 第 2 层：纯本地读取，启动零网络
  ↓
hot/pack/（GitHub Pages）      ← 第 3 层：启动成功 3 秒后才联网
```

- 资源包是 **zip**，由原生桥 `AndroidHot.installPack()` 落盘到 `files/hot/`，
  再用虚拟域 `https://local.hot/` 读取 —— 不经过 localStorage，**没有 5MB 配额限制**，图片音频随便加。
- 玩法文件在清单里单独标记（`games` 数组），`boot.js` 把它们**插在 `js/games.js` 之后、`js/app.js` 之前**加载，
  所以首页第一次渲染就能看到新玩法。这段逻辑已经写死在冻结的 `boot.js` 里 —— 以后加玩法**永远不用再动 `boot.js`**。

### 四道保险（热更出问题也不会白屏）

| 保险 | 行为 |
|---|---|
| 启动路径零网络 | 网络只发生在启动成功后的后台，失败最多是「这次没更新上」 |
| 1.5 秒哨兵 | 没渲染出 `#app` 就判失败 → 标记坏包 → 回滚 → 重载 |
| 连续 2 次失败 | 永久关闭热更，退回内置版本 |
| 逃生开关 | 访问 `index.html#nohot` 或 `index.html?safe=1` 强制走内置 |

调试：设置面板连点标题 5 次 → 打开 `#hotlog` 看完整启动日志，不用 adb。

---

## 4. 发布一版热更的完整命令

```bash
# 1. 改内容（数据 / 玩法 / 图片 / 样式，随便改）
# 2. 生成资源包
node tools/gen-pack.mjs

# 3. 同步进 Android 壳 assets（只有要出新 APK 时才需要）
./sync-assets.sh --apply

# 4. 出新 APK（改了 boot.js / 原生代码 / 版本号时才需要）
./build-apk.sh

# 5. 推送
git add -A && git commit -m "说明" && git push
```

---

## 5. 常见疑问

**Q：改了 `js/games.js` 里的一个玩法，也要走这套吗？**
要，但只需 `node tools/gen-pack.mjs`（不用新建文件）。现有玩法已经在 `games.js` 末尾通过 `registerGame` 注册，改逻辑照样热更。

**Q：用户什么时候看到新内容？**
不是即时。App 打开 → 启动成功 3 秒后后台下载 → **下一次打开**才生效。这是刻意的：绝不让网络影响本次启动。

**Q：能强制立刻生效吗？**
真机上把 App 切到后台再切回来（或杀掉重开）即可触发下一轮启动。

**Q：万一新玩法把 App 搞崩了？**
哨兵 1.5 秒内没渲染出首页 → 自动标记这个 build 为坏包 → 回滚到内置版本 → 重载。用户只会感觉到闪一下，不会白屏。

**Q：热更包有大小限制吗？**
没有硬限制。代码包（js+css）通常几十 KB，资源包（图片音频）单独一个 zip，几 MB 也照下。

## 不只是玩法：新增普通 js 模块也能热更

玩法（`js/game-*.js`）有专门的扫描通道，但**不是玩法的新文件同样能热更**——`boot.js` 的 `planFiles()` 会把资源包里 `BUILTIN` 清单之外、非玩法、非 `css/style.css`、非 `img/` 的文件**追加到末尾加载**。

所以想加一个公共模块（例如夸奖与满分特效 `js/praise.js`），流程跟加玩法一样简单：

1. 写 `js/praise.js`，挂到 `window.xxx` 上（文件末尾加载，运行时调用完全没问题）；
2. 在 `js/app.js` / `js/games.js` 里调用它，**务必做存在性判断**——万一这个文件没加载上，只是没有特效，不能白屏：
   ```js
   var P = window.PRAISE;
   var lv = P ? P.level(acc, earned) : "";
   var head = P ? P.block(lv) : '<div style="font-size:46px">🎉</div>';
   ```
3. `./publish.sh` 一条命令：生成热更包 → 推 gh-pages。**不出 APK**。

> CSS 也能热更（`css/style.css` 会被顶掉内置的），但纯新增的样式更省事的做法是在 js 里注入一段 `<style>`，不碰 CSS 文件、不依赖样式通道。

**本次案例（2026-09-19）**：三科同步加了「满分特效 + 夸奖语」。满分/全对 → 金色光环 + 46 片彩带 + 顶部横幅 + 三颗大星 + 随机夸奖语并朗读；三星 → 26 片彩带 + 夸奖语；挑战类没有满分概念，用「打破纪录」当最高光。全部落在 `js/praise.js`（新增）、`js/app.js`、`js/games.js`，热更下发，APK 版本号没动。

---

## 6. 排坑实录（已验证，2026-09-21）

> 以下都是**真机上验证过**的结论，不是推测。每条都标了「现象 → 根因 → 修法」。

### 坑 1：图片整片回退成占位图（路径缺学科前缀）

- **现象**：玩法里 `IMG = "…/img/battle/"`，实际图片 404，全回退成占位图。
- **根因**：`cn/js/games.js` 规定图片**必须带学科前缀**，正确路径是 `math/img/battle/`（或 `cn/`、`en/` 对应前缀）。写 `img/battle/` 会指向不存在的路径。
- **修法**：所有玩法内图片 URL 一律写 `https://local.hot/<学科>/img/<玩法>/…`；图片文件也按同样前缀放进对应学科 `img/` 目录。

### 坑 2：`gen-pack` 报「未登记到 boot.js」是误报

- **现象**：跑 `gen-pack.mjs` 时控制台提示某个 `game-*.js`「未登记到 boot.js」。
- **根因**：这是**误报**。新玩法走的是 `MANIFEST.games[]` + `m.games` 自动注入通道，根本不靠 `boot.js` 里的 `BUILTIN` 清单。
- **结论**：无视这条提示。只要 `registerGame({id})` 写对、清单里能看到该玩法 id，就能正常上线。

### 坑 3：触屏电视一体机「乱成一团」（body.tv 没加）

- **现象**：大屏触屏电视上整体布局错乱（其实是跑了手机版布局），不是单某个玩法的问题。
- **根因**：判定链是 `AndroidDevice.isTV()` → `boot.js` 加 `body.tv/phone/tablet` → `tv.js detectTV()` → `css/tv.css`（`body.tv` 前缀）生效。
  触屏电视一体机：原生桥 `isTV()` 三重判定（非 TV UI 模式 / 无 LEANBACK / **有触屏**）全落空 → 返回 false → `boot.js` 因 `swDp≈361 < 600` 判成 `phone` → `tv.js` 的 `detectTV()` 早退 → `body.tv` **从未加** → `tv.css` 全废。
- **修法（已上线）**：`tv.js` 加**几何复核兜底**——原生桥 false 时，若「大屏（短边 dp ≥ 600，或物理短边 ≥ 900px）+ 横屏 + 宽高比 1.4~2.0」仍判为 TV（补 `body.tv` 并移除 `phone/tablet`）。`tv-tune.js` 里 `tvForce:true` 可强制开，`#phone` 反向开关可强制回手机版。`tv.js` 本身可热更，不用出 APK。
- **排查入口**：热更自检面板 → `tvDiagHtml()` 会显示「电视版样式：✅已启用 / ❌未启用」+ 屏幕宽高比 + 原判定 `sw`，一眼定位。

### 坑 4：「强制重新下载」按钮原来只清标记、不下载（有害）

- **现象**：热更自检面板里点的「清除数据 / 强制重新下载」后，反而退回内置旧版，新内容更出不来。
- **根因**：原 `hotForceReload` 实现只调了 `AndroidHot.reset()`（**只清更新标记、不触发下载**，等于把已下好的包作废）。
- **修法（已上线）**：`hotForceReload` 改调 `window.hotNowCheck()`（真正后台下载）。按钮文案改为「⬇️ 立即下载更新」，并加了 `#hotNowMsg` 进度区。
- **关键澄清**：电视 / 手机热更**本身全自动**——App 启动成功 3 秒后后台静默下载，**下次打开生效**。**不需要**手动点「清除数据」，点清除数据反而可能把已下好的包清掉。

### 坑 5：游戏圈分数输光后卡死、无反应

- **现象**：知识圈竞赛（arena2）星星输光后，老师不批评鼓励、不回教室、游戏不结束，整个停住。
- **根因**：输光星后 `afterResolve` 调用 `endGame()` 切回教室并结算，但 `endGame()` **原先未定义**，抛 `ReferenceError`，被 `later()` 的 `try/catch` 静默吞掉 → 卡死。
- **修法（已上线）**：补 `endGame()`——切回教室 + 老师评价 + 结算面板。老师点名对所有输家逐人播报（`speak(... ,"zh-CN")` 运行时免费女声，同轮错峰 1.4s）。
- **通用提醒**：任何走 `later()/every()` 的回调里抛异常都会被吞，**务必保证回调调用的函数都已定义**。

### 坑 5.1：arena2 胜负规则（2026-09-21 二次修正，已被用户推翻过一次）

- **第一版误设计**：真人掉星保底留 1 颗（永不淘汰）+ 第 5/6/7 局强制淘汰 1/2/3 个 AI + 真人永远第一。→ 用户明确说「不对」。
- **正确规则（当前生效）**：
  1. 每人 **5 颗星**（`START_STARS=5`）；真人答错掉光 5 星也**出局**——走回教室 + 老师批评 + 动画走完游戏结束（无「受保护」）。
  2. 共 **8 题**；淘汰只发生在某人星掉到 0（自然发生），**不按局数强制淘汰 AI**。
  3. 8 题后按星判定：满星（5/5）→ 真人必第一、**受表扬**（👑 知识王者 + 彩带）；不满星 → 真人星 ≥ 存活 AI 最高星则真人第一、**但被批评**（没拿满星）；真人星 < 存活 AI 最高星 → **AI 赢、真人被批评**。
  - 一句话：只有「满星」才受表扬；其余情况真人即便拿第一也要被批评，AI 星更多则 AI 赢。
- **实现要点**：`renderOver` 里 `meFull = me.stars>=START_STARS`；`youPraised=meFull`；`championFX()` 仅 `youPraised` 时触发；`arenaTeacherTalk`/`endGame` 的语音也按 `meFull`/`S._praised` 分流。

### 坑 6：`readInv` 读空库存得到 `兑换(undefined)`

- **现象**：背包显示「兑换(undefined)」「附件 NaN」。
- **根因**：原 `readInv` 用 `JSON.parse("{}")`，返回 `{}` 是真值 → 直接 `return`，没给字段补默认 → `attachments` 等字段是 `undefined`。
- **修法（已上线）**：逐字段补默认（`v.attachments = Math.max(0, num(v.attachments, 0))` 等），`{}` 与 `null` 都走默认分支。

### 坑 7：新玩法只在某一科出现（部署遗漏）

- **现象**：数学科首页出现了新玩法，语文、英语没有。
- **根因**：`registerGame` 只扫描**当前学科**目录下的 `js/game-*.js`。文件只放进 `math/js/`，自然只有数学有。
- **修法**：见 §2 第 4 步，三科各放一份同一 id 的文件再 `gen-pack`。

### 坑 8：头像在窄屏溢出卡片框

- **现象**：对战头像固定 62px，在窄屏（尤其非 TV 的小屏）把卡片撑破、文字被挤掉。
- **根因**：`.a-avatar` 写死固定宽度，不随容器收缩。
- **修法（已上线）**：`.a-row` 改 `flex`，`.a-avatar{flex:1 1 0;min-width:0;max-width:62px}`，`.a-body{width:100%;max-width:54px;aspect-ratio:1/1}` 自适应收缩。
- **已知限制**：`aspect-ratio` 在安卓 8 上整条失效（游戏内样式，非 `tv.css`），属兼容性遗留，暂未处理。

---

## 7. 2.5.0 通用空壳 与 2.4.2 兼容（2026-09-21）

### 7.1 什么是「2.5.0 空壳」

- **2.4.2 APK 是「语文单科壳」**：内置 boot.js 只登记语文一科，内容上也不含数学/英语。
- **2.5.0 APK 是「通用空壳」**：`js/boot.js` 的 `SUBJS` 登记了 **语文/数学/英语** 三科，学科页由 `js/subject.js` 动态列出，**APK 本身不锁定任何一科**。
- **内容全部走热更**：`tools/gen-pack.mjs` 把三科的 `js / css / 图片 / 音频` 整包打出（本次包 `build=1c564edaaa5a2390`，72 个代码文件 + 160 个资源文件，`arena2` 三科各注册一次）。设备启动 3 秒后后台装上，下次打开即生效，**无需出新 APK**。

### 7.2 怎么「出」这个空壳

```bash
# Web 层 → 壳工程 assets（防两份代码脱节，见 §11.10）→ Gradle 构建 → 签名
bash build-apk.sh
# 版本号在 chinese-universal/app/build.gradle：versionName "2.5.0" / versionCode 12
# boot.js 是冻结文件，绝不进热更包（gen-pack 会主动剔除它）
```

> 只要 `SUBJS` 里登记的三科齐全、资源包覆盖三科，这个 APK 就是「通用空壳」——后续任何内容更新都只发资源包。

### 7.3 2.4.2 必须继续兼容（两层保险）

| 层 | 机制 | 说明 |
|---|---|---|
| 门槛 | `min_apk` 恒为 **3** | `gen-pack` 的 `DEFAULT_MIN_APK=3`，**绝不跟着新 APK 的 versionCode 自动抬高**（历史事故：曾因 versionCode 提到 12，导致 apk=11 的 2.4.2 整份拒收）。要抬门槛只能显式 `--min-apk N` 并在提交里写清原因。 |
| 兼容桩 | `legacy/` 根级 js 桩随包下发 | `js/app.js`（选学科看门人）、`js/bridge.js`（桥接层）、`js/game-battle.js` 等空壳，让 2.4.2 那套只认根级 `js/*.js` 的老 boot.js 也能渲染选学科页并加载三科。 |

- **验证兼容**：解 `hot/pack/code.*.zip` 里的 `MANIFEST.json`，确认 `min_apk===3`、`games` 含三科 `arena2`、且 `files` 里含 `js/app.js / js/bridge.js / js/game-battle.js` 等根级桩。
- **改包铁律**：`min_apk` 只能**向下兼容**；新增玩法/科目永远走热更，不要为它们动 boot.js（改 boot.js = 必须出新 APK）。

### 7.4 本轮 arena2（知识圈竞赛）随空壳一起热更的内容

- 改名：`知识圈竞赛2` → `知识圈竞赛`（id 仍 `arena2`）。
- 删 v1「知识圈」(`battle`)：三科 `js/game-battle.js` 已删，`boot.js` 的 `BUILTIN_CN` 同步移除该条目（仅保留 `legacy/js/game-battle.js` 给 2.4.2）。
- 语文：删「看图识字」(`picture`)、「诗句填空」(`poemfill`)，保留「连句成诗」(`poemsort`)。
- 英语：删「翻牌配对」(`memory`)、「看图识词」(`picture`)。
- 开场前 **30 秒选角色**：人类优先选头像，超时自动选第一个；选中的角色排到 0 号位当真人。
- 输家 **走进左侧教室区并缩小**（左右两片同屏，不再移出屏外）；AI 星星**每题随答错递减并闪一下变灰**；**仅满星夺冠才发配件碎片**。

---

### 附：已被推翻的无效推论（别再信）

| 曾误判 | 真相 |
|---|---|
| 「老师只点名第一个输家」 | 代码本就对所有输家逐人播报，是早先看串行导致的误读 |
| 「CI 没把玩法部署上去」 | 设备 build = CI build（`d3c9943de206b79d`），`code.zip` 含 `game-battle-v2.js` 且 `MANIFEST` 有 arena2，部署成功 |
| 「电视端热更从没成功」 | 根因是坑 4 的按钮只清标不下载；热更链路本身正常。把按钮改真下载后，电视/手机走同一条全自动链路 |
| 「后台轮询脚本报『未上线』」 | 纯属轮询脚本 `WANT` 变量算错（用了旧 build）+ 沙箱 jsDelivr 时通时断，非部署问题 |

**本次案例（2026-09-21）**：上线「知识圈竞赛2（arena2）」——真实 IP 学生名、老师对每个输家点名（免费 TTS 女声）、输光星自动回教室 + 老师评价 + 结算；修掉输光卡死（坑 5）、兑换 undefined（坑 6）、头像溢出（坑 8）、触屏电视乱布局（坑 3）、热更按钮只清标不下载（坑 4）；三科同步上线（坑 7）；补齐声效与英语 TTS、按学科出题（数学/语文/英语各出本科目题）、操场高度与选项溢出修复。二次修正胜负规则（坑 5.1）：5 星、真人可被淘汰、满星必第一受表扬 / 不满星按星比且被批评。全部热更下发，APK 版本号未动。

**本轮追加（2026-09-21 晚）**：① 出 **2.5.0 通用空壳**——`boot.js` 登记语文/数学/英语三科、`subject.js` 动态列三科，内容全走 `gen-pack` 热更包（`min_apk=3` 保 2.4.2 兼容，见 §7）；② arena2 改名「知识圈竞赛」，删 v1「知识圈」(`battle`) 三科文件；③ 语文去「看图识字/诗句填空」、英语去「翻牌配对/看图识词」；④ 开场前 **30 秒选角色**（人类优先选头像、超时自动选第一个）；⑤ 输家**走进左侧教室区并缩小**、AI 星星**每题随答错递减且闪一下变灰**、**仅满星夺冠才发配件碎片**。均热更下发。
